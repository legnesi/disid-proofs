// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.disid.restful.web;

import com.disid.restful.model.Product;
import com.disid.restful.service.api.ProductService;
import com.disid.restful.web.ProductFormatter;
import java.text.ParseException;
import java.util.Locale;
import org.springframework.core.convert.ConversionService;
import org.springframework.format.Formatter;
import org.springframework.util.StringUtils;

privileged aspect ProductFormatter_Roo_Formatter {
    
    declare parents: ProductFormatter implements Formatter<Product>;
    
    public ProductService ProductFormatter.productService;
    
    public ConversionService ProductFormatter.conversionService;
    
    public ProductFormatter.new(ProductService productService, ConversionService conversionService) {
        this.productService = productService;
        this.conversionService = conversionService;
    }

    public Product ProductFormatter.parse(String text, Locale locale) throws ParseException {
        if (text == null || !StringUtils.hasText(text)) {
            return null;
        }
        Long id = conversionService.convert(text, Long.class);
        return productService.findOne(id);
    }
    
    public String ProductFormatter.print(Product product, Locale locale) {
        return product == null ? null : new StringBuilder().append(product.getName()).append(" - ").append(product.getDescription()).toString();
    }
    
}
